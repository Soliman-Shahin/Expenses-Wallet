{
  "android": {
    "buildCommand": "npm install --include=optional --legacy-peer-deps"
  },
  "ios": {
    "buildCommand": "npm install --include=optional --legacy-peer-deps"
  },
  "web": {
    "buildCommand": "npm install --include=optional --legacy-peer-deps"
  }
}
